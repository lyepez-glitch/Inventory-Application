doctype html
html(lang = 'en')
 head
   title Home
 body
   h1 This is the #{cat.name} page
      h1 #{cat.description}

      ul
        each item in items
          li #{item.name}
          |#{item.description}
   a(href=itemURL) Add Items Here
